# Iot2026_Basic_Cpp
Iot 개발자과정 CPP 기초프로그래밍

### 배열포인터 / 포인터배열

1. 배열포인터 (Pointer to Array)
    - 정의
        - 배열 전체를 가리키는 포인터

    - 선언
        - int (*p)[5];
        - → p는 "int 5개짜리 배열을 가리키는 포인터"

    - 예시
        - int arr[5];
        - int (*p)[5] = &arr;

    - 특징
        - p + 1 → 배열 전체 크기만큼 증가
        - 연속 메모리 구조
        - 2차원 배열 처리에 사용

2. 포인터배열 (Array of Pointers)
    - 정의
        - 포인터들을 저장하는 배열

    - 선언
        - int *p[5];
        - → p는 "int형 포인터 5개를 저장하는 배열"

    - 예시
        - int a, b, c;
        - int *p[3] = { &a, &b, &c };

    - 특징
        - p + 1 → 포인터 1개 크기 증가
        - 각 요소는 주소값 저장
        - 문자열 배열에서 많이 사용


3. 차이 비교

| 구분 | 배열포인터 | 포인터배열 |
|------|------------|------------|
| 선언 | int (*p)[5] | int *p[5] |
| 의미 | 배열을 가리킴 | 포인터를 저장 |
| 우선순위 | * 먼저 | [] 먼저 |
| 주 사용 | 2차원 배열 | 문자열 처리 |


## 핵심 정리
    - 배열포인터 = 배열을 가리키는 포인터
    - 포인터배열 = 포인터들의 배열
    - 괄호 위치가 모든 차이를 만든다



### 메모리 영역

# 전역변수 / 지역변수 / static / const / 배열 / 포인터의 메모리 위치

## 1. 프로그램 메모리 구조

프로그램이 실행되면 메모리는 크게 다음 영역으로 나뉜다.

1) Code 영역 (Text 영역)
2) Data 영역
3) Heap 영역
4) Stack 영역

---

## 2. Code 영역 (Text Segment)

- 함수 코드가 저장되는 영역
- 읽기 전용 (보통 수정 불가)
- 프로그램 시작부터 종료까지 유지

예)
    int func() { return 1; }

    printf("%p\n", func);

→ func 함수의 주소는 Code 영역 주소

---

## 3. Data 영역

프로그램 시작과 동시에 할당
프로그램 종료 시 해제

### (1) 초기화된 전역변수 / static 변수

    int global_c = 100;
    static int c;

→ Data 영역에 저장

### (2) 초기화되지 않은 전역변수

    int a;

→ BSS 영역 (Data 영역의 일부)

※ BSS : 0으로 자동 초기화되는 영역

---

## 4. Stack 영역

- 함수가 호출될 때 생성
- 함수 종료 시 자동 해제
- 지역변수 저장

예)

    int main()
    {
        int b;
        const int d;
        char arr[20] = "hi";
    }

→ b, d, arr 모두 Stack 영역

특징
- 함수 호출 시 위에서 아래로 쌓임
- 속도 빠름
- 크기 제한 존재
- Stack Overflow 발생 가능

---

## 5. Heap 영역

- 동적 메모리 할당 영역
- malloc, calloc, realloc 사용
- 직접 free 해야 함

예)

    int *p = malloc(sizeof(int));

→ Heap 영역에 생성

---

## 6. 문자열과 포인터의 차이

코드:

    char arr[20] = "hi";
    char* parr = "hong";

### arr

- 배열 자체는 Stack 영역
- "hi" 문자열 내용도 배열 안에 복사됨
- 수정 가능

### parr

- 포인터 변수는 Stack 영역
- "hong" 문자열은 읽기 전용 영역 (Code 영역 근처의 상수 영역)
- 문자열 리터럴은 수정 불가

정리

| 구분 | 포인터 변수 | 실제 문자열 |
|------|------------|------------|
| arr  | Stack | Stack |
| parr | Stack | 읽기전용 영역 |

---

## 7. 변수별 메모리 정리

| 변수 | 저장 영역 |
|------|-----------|
| func | Code |
| a (전역) | BSS |
| global_c | Data |
| b (지역) | Stack |
| c (static) | Data |
| d (const 지역) | Stack |
| arr | Stack |
| parr | Stack (문자열은 읽기전용 영역) |

---

## 8. 한눈에 그림 정리

높은 주소
-----------------
Stack  ← 지역변수
-----------------
Heap   ← malloc
-----------------
Data   ← 전역, static
BSS    ← 초기화 안된 전역
-----------------
Code   ← 함수
-----------------
낮은 주소

---

## 핵심 정리

- 전역 / static → Data 영역
- 초기화 안된 전역 → BSS
- 지역변수 → Stack
- malloc → Heap
- 함수 → Code
- 문자열 리터럴 → 읽기 전용 영역

프로그램은 이렇게 메모리 구조 위에서 동작한다.



### ----- 위에 글들 재정리 -----

# IoT 개발자 과정 C++ 기초

## 배열포인터 vs 포인터배열

### 배열포인터 (Pointer to Array)
- 배열 전체를 가리키는 포인터
- 특징
    - 배열 전체 크기 단위로 이동 (`p + 1`)
    - 연속 메모리 구조
    - 2차원 배열 처리에 사용
- 선언 예시
    - `int (*p)[5];` → 5개 int 배열을 가리키는 포인터
    - 사용: `p = &arr;`

### 포인터배열 (Array of Pointers)
- 포인터들을 저장하는 배열
- 특징
    - 각 요소는 주소값 저장
    - p + 1 → 포인터 1개 크기 증가
    - 문자열 배열 처리에 자주 사용
- 선언 예시
    - `int *p[5];` → int형 포인터 5개 저장

### 차이 요약

| 구분 | 배열포인터 | 포인터배열 |
|------|------------|------------|
| 선언 | int (*p)[5] | int *p[5] |
| 의미 | 배열 가리킴 | 포인터 저장 |
| 우선순위 | * 먼저 | [] 먼저 |
| 주요 사용 | 2차원 배열 | 문자열 처리 |

### 핵심 정리
- 배열포인터 = 배열 전체를 가리키는 포인터
- 포인터배열 = 포인터들의 배열
- 괄호 위치가 의미를 완전히 바꿈

---

## 프로그램 메모리 구조

### 1. 메모리 영역 구분
프로그램 실행 시 메모리는 크게 4개 영역으로 나뉨:
1. Code 영역 (Text Segment) – 함수 코드
2. Data 영역 – 초기화된 전역/Static 변수
3. Heap 영역 – 동적 메모리
4. Stack 영역 – 지역변수

---

### 2. Code 영역
- 함수 코드가 저장되는 영역
- 읽기 전용
- 프로그램 시작부터 종료까지 유지
- 예: `func` 함수 주소 → Code 영역

---

### 3. Data 영역
- 프로그램 시작과 동시에 할당, 종료 시 해제
- 초기화된 전역/Static 변수 저장
- 초기화되지 않은 전역 변수 → BSS 영역
- 특징: 자동으로 0 초기화되는 BSS 존재

---

### 4. Stack 영역
- 함수 호출 시 생성, 종료 시 자동 해제
- 지역변수, 함수 매개변수 저장
- 특징
    - LIFO 구조, 속도 빠름
    - 크기 제한, Stack Overflow 가능

---

### 5. Heap 영역
- 동적 메모리 할당 영역
- malloc, calloc, realloc 사용
- 사용 후 반드시 free 필요
- 특징: 용량 유동적, 속도 Stack보다 느림

---

### 6. 문자열과 포인터 차이

| 구분 | 배열 변수 | 포인터 변수 |
|------|-----------|-------------|
| 메모리 | Stack | Stack (문자열은 읽기 전용) |
| 수정 가능 | 가능 | 문자열 리터럴은 불가 |

- 배열: 문자열 내용이 Stack에 복사됨 → 수정 가능  
- 포인터: 문자열 리터럴을 가리킴 → 읽기 전용

---

### 7. 변수별 메모리 정리

| 변수 | 저장 영역 |
|------|-----------|
| func | Code |
| a (전역) | BSS |
| global_c | Data |
| b (지역) | Stack |
| c (static) | Data |
| d (const 지역) | Stack |
| arr | Stack |
| parr | Stack (문자열은 읽기 전용) |

---

### 8. 한눈에 그림 정리

높은 주소
-----------------
Stack  ← 지역변수
-----------------
Heap   ← malloc 등
-----------------
Data   ← 전역, static
BSS    ← 초기화 안된 전역
-----------------
Code   ← 함수
-----------------
낮은 주소

---

### 핵심 정리
- 전역/Static → Data 영역  
- 초기화 안된 전역 → BSS  
- 지역변수 → Stack  
- malloc → Heap  
- 함수 → Code  
- 문자열 리터럴 → 읽기 전용 영역  

- 프로그램은 위 메모리 구조 위에서 동작









### ----- 260227 -----
1. C++의 입출력
2. 함수 오버로딩
3. 함수 디폴트 매개변수
4. 함수의 인라인화
    인라인화 : 보통 함수를 호출하면 함수의 정의부로 찾아가서 계산하고 다시 돌아오는데,
              인라인화를 하면 함수호출하는 그 곳이 바로 정의부가 되어버린다.
              그래서 정의부를 찾아가지 않아도 되므로 속도가 빠르다고 볼 수 있다.

              ⚠️ 하지만 항상 빠른 건 아님

함수가 너무 크면 코드가 많이 복사돼서 코드 크기 증가

코드가 커지면 캐시 효율이 떨어질 수 있음

그래서 컴파일러가 자동으로 판단해서 인라인 여부를 결정함

예: C/C++의 inline 키워드는 "반드시 인라인하라"가 아니라
👉 "인라인을 권장한다"는 힌트에 가까움

5. namespace 
namespace안에 namespace를 만들어서 중첩도 가능

6. 클래스란
- 공통적인 속성인 변수와 그 변수를 사용하여 실행되는 기능으로 이루어진것
- 연산자 오버로딩을 알아볼것
- 오버로딩은 함수 다중 정의
- 오버라이딩은 함수 재정의

7. 포인터에서 const 키워드 선언 위치에 따른 의미
8. 제네릭, 템플릿
9. bool 자료형
10. 참조자(reference)



### C++ 입출력 (I/O)
- 콘솔로 데이터를 주고받는 기본 방법
- 주요 객체
    - `cin` : 키보드 입력
    - `cout` : 화면 출력
    - `cerr` : 에러 출력
    - `clog` : 로그 출력
- 특징
    - 출력 버퍼 사용 → 속도 최적화 가능
    - `endl` : 줄바꿈 + flush
    - `'\n'` : 단순 줄바꿈, 속도 빠름
- 주의: 버퍼 동기화를 조절하면 성능 차이가 있음

### 함수 오버로딩
- 같은 이름의 함수를 매개변수 타입/개수로 구분하여 정의
- 조건
    - 이름 동일, 매개변수 달라야 함
    - 반환형만 다른 경우는 허용 안됨
- 장점: 코드 가독성 향상, 다양한 입력 처리 가능
- 주의: 기본 매개변수와 함께 쓰면 모호성 가능

### 디폴트 매개변수
- 함수 매개변수에 기본값 지정 → 생략 가능
- 특징
    - 오른쪽에서 왼쪽 순서로 지정
    - 선언부에만 지정하는 것이 관례
- 장점: 호출 편리, 코드 간결

### 함수 인라인화
- 호출 시 정의부 코드가 삽입되도록 권장
- 장점: 호출 오버헤드 제거 → 속도 향상
- 단점
    - 함수가 크면 코드 복사로 프로그램 크기 증가
    - 캐시 효율 저하 가능
- 핵심: `inline`은 강제가 아닌 권장 의미

### namespace
- 이름 충돌 방지, 코드 그룹화
- 특징
    - 중첩 가능
    - `using namespace`로 편리하게 사용
    - 전역 이름과 충돌 방지

### 클래스
- 변수(속성) + 함수(동작) 묶음 → 사용자 정의 타입
- 특징
    - 캡슐화 : `public`, `private`, `protected`
    - 연산자 오버로딩 : 같은 연산자 여러 정의 가능
    - 함수 오버라이딩 : 상속 시 부모 함수 재정의
- 장점: 객체 지향 프로그래밍 핵심

### 포인터와 const
- `const int *p` : 값 변경 불가
- `int *const p` : 주소 변경 불가
- `const int *const p` : 값과 주소 모두 변경 불가
- 장점: 안전한 코드 작성, 실수 방지

### 제네릭 / 템플릿
- 타입에 독립적인 함수/클래스 작성
- 특징
    - 재사용성 ↑
    - 다양한 타입 처리 가능
    - 구현은 헤더 파일에 작성
- 장점: 코드 반복 최소화

### bool 자료형
- 참/거짓 저장 (`true`, `false`)
- 특징
    - true → 1, false → 0
    - 다른 타입 자동 변환 가능
    - 최소 1 byte
- 용도: 조건 판단, 플래그 설정

### 참조자 (Reference)
- 변수의 별칭, 별도의 메모리 없이 원본 참조
- 특징
    - 값 전달 최적화
    - 상수 참조 가능 : `const int &r`
    - 포인터와 달리 NULL 참조 불가
- 장점: 코드 간결, 성능 최적화

### 핵심 정리
- 입출력 : cin/cout, 버퍼 고려
- 함수 : 오버로딩, 디폴트, 인라인
- 이름 충돌 방지 : namespace
- 객체지향 : 클래스, 오버로딩/오버라이딩
- 안전한 포인터 : const 활용
- 재사용성 : 템플릿
- 단순 참/거짓 : bool
- 값 전달 최적화 : 참조자




### ----- 260303 -----
1. 래퍼런스(참조자)는 뭔가를 저장하는 공간이 메모리에 새로 할당되는것이 아니다.
    - 컴파일 당시에 래퍼런스 자체가 주소값으로 그대로 대체되는 것이다.
    - 따라서 상수값 자체도 래퍼런스 할 수는 있다.
        ex) const int& ref = 4;  or  int&& ref = 4; 

2. 클래스
    - 클래스는 변수들과 함께 함수를 포함한 내가 만드는 사용자 정의 자료형
    - 클래스 객체를 생성할때 생성자 선언을 할수있다.
    - 생성자를 매개변수를 받는 형태로 내가 만들어두었으면 클래스 객체 생성시 그 형태대로 선언을 해줘야하며,
    - 생성자를 만들어두지 않았다면 그냥 객체생성만 하거나, 혹은 변수 갯수에 맞게끔 함수형태로 객체 생성을 해주면 자동으로 변수가 초기화되는
    - 생성자가 실행된다. 
    - 생성자는 한 클래스에 여러개 만들어둘수있다. 매개변수가 없는 형태로 내가 만들어두면 매개변수 없이 생성할때 그 생성자가 실행되고,
    - 매개변수가 있는 형태의 생성자가 있으면 매개변수를 넣어주는형태의 객체 생성시 그 생성자가 실행된다.
    - 원래는 래퍼런스나 상수는 선언과 동시에 초기화해야하는데, 생성자의 정의부분에 들어가면 초기화가 불가능하다.
    - 그래서 생성자 자체에서 정의부에 들어가기전에 콜론초기화를 사용하여 상수와 래퍼런스를 초기화 가능하다.

    - 클래스가 다른 클래스의 객체를 변수로 가질수 있다.
    - 만약 객체 변수가 생성자를 가지고 있는데, 이 클래스에서 생성자시에 콜론초기화로 객체생성자문구를 사용하면 객체가 초기화된다.
    


3. const로 선언되어있는 객체를 대상으로는 const로 선언되지 않는 멤버함수의 호출이 불가능하다.


4. friend 선언으로 한 클래스는 private멤버 변수나 함수에 접근할수 있게끔 한다.
    따라서 private이라는 정보은닉에 반하는 선언이기 때문에 매우 제한적으로 선언되어야 한다.


5. 클래스에 선언된 static 멤버 변수는 각 객체별로 가지고 있는 변수가 아니고 클래스의 전체적인 변수이다.
    따라서 그 클래스로 생성된 객체들이 static변수 하나를 공유해서 사용한다.
    정리하자면 각 객체의 멤버변수가 아니고, 클래스 멤버 변수이다.



### ----- 260306 -----  // 이때부터는 안성주쌤이 아프셔서 다른 강사님이 오셨다.
1. 프로그래밍 언어의 Schema
    - 모든 프로그래밍 언어는 Input, Output, 그리고 저 둘의 사이에 Processing이 있다.
    - 그리고 Processing에는 연산(Operator), 조건(Condition), 반복(Loop)이 있다.


### ----- 260309 -----

1. 클래스에서 static 멤버변수는 반드시 클래스 외부에서 초기화 해야한다. 이러면 싱글톤처럼 이용 가능한듯?
    ex) static int totalPlayers;
        외부에서 int Player::totalPlayers = 0;

2. 정적 멤버 함수 : 클래스의 멤버 함수이지만 static으로 선언된 함수이기 때문에 프로그램이 종료되기전까지 존재하는 함수이다.
    원래 멤버 함수는 객체가 생성되고나서 그 객체가 사용할수있는 함수이지만 static으로 존재하기에 따로 객체를 생성하지 않아도
    main문에서 사용할 수 있다.(당연하지만 객체가 사용할 수 있음)
    ex) class Math의 멤버 함수 static int add();
        - main문에서 Math::add()로 객체를 생성하지 않고도 사용가능.
    추가적으로 static 멤버 함수는 특정 객체에 속하는 함수가 아니라 클래스 자체의 함수이다. 
    static으로 선언된 멤버 함수는 객체의 멤버 변수에 접근 할 수없다. 왜냐하면 객체의 멤버 변수는 객체가 생성되고 나서 생성되는 변수인데,
    static멤버 함수는 객체가 생성도 되기 전에 이미 함수영역에 저장되는 함수이다. 따라서 정의문에 있는 멤버함수는 아직 뭔지 모르기에 함수영역에 올라가지를 못한다.
    즉, 클래스의 함수이지만 그 클래스의 변수를 사용한 동작은 불가능하다.
    그러나 함수에서 사용할 멤버 변수가 static이면 이것도 클래스라는것이 컴파일 될때 바로 생성되는 변수이므로 static함수도 이 변수를 인지할 수 있다.
    따라서 static으로 선언된 멤버 변수를 static멤버 함수안에 사용하는것은 가능하다.
    참고로 static멤버 변수도 cout << Math::radius << endl; 이런식으로 사용 가능하다. 
    추가로 static과 const는 함수에 동시 선언이 불가능하다. static으로 선언된 함수라는 것은 클래스의 멤버 변수가 올 수 없다고 하였지만 const의 경우는 
    객체의 멤버변수를 변경하지 않겠다는 뜻이다. 따라서 하나는 멤버 변수가 올수 없는데, 하나는 멤버변수가 있다고 하니 둘이 말이 맞지 않는다.
    static 멤버 변수를 사용한 static함수라고 해도 const는 사용할 수 없다. 

     
    아래는 정적 멤버 함수에 대한 똑같은 내용인데, gpt가 정리해 준 내용

    // ==============================
// 정적(static) 멤버 함수 정리
// ==============================

// 1️⃣ 정의와 존재 시점
// - 클래스의 멤버 함수이지만 static으로 선언된 함수이기 때문에 프로그램 종료 전까지 존재.
// - 일반 멤버 함수는 객체가 생성된 후 호출 가능하지만, static 함수는 객체 없이도 호출 가능.
// 예:
class Math {
public:
    static int add(int a, int b);
};
// main에서 호출
Math::add(1, 2); // 객체 생성 없이 사용 가능
// 참고: 객체로 호출도 가능하지만, 실제로 객체는 잘 사용되지 않음.
Math m;
m.add(1, 2); // 가능하지만 권장하지 않음

// 2️⃣ 객체와 관계 없음
// - static 멤버 함수는 특정 객체에 속하지 않고 클래스 자체에 속함
// - this 포인터가 존재하지 않음
// - 따라서 객체 멤버 변수에는 접근 불가

// 3️⃣ 객체 멤버 변수 접근 불가 이유
// - 일반 멤버 변수는 객체가 생성될 때 생성됨
// - static 멤버 함수는 객체와 관계없이 프로그램 시작 시 메모리에 존재
// - 따라서 정의문 안에서 일반 멤버 변수를 사용할 수 없음
// - 즉, 클래스 함수이지만 객체 멤버 변수를 사용하는 동작은 불가능

// 4️⃣ static 멤버 변수 사용 가능
// - static 멤버 변수는 클래스 영역에 존재하며 프로그램 시작 시 생성됨
// - 따라서 static 멤버 함수 안에서 접근 가능
class Circle {
public:
    static double radius;
    static double getArea() {
        return radius * radius * 3.14; // OK
    }
};
// main에서 사용
std::cout << Circle::radius << std::endl;

// 5️⃣ static과 const의 관계
// - static과 const는 함수 선언에서 동시에 사용할 수 없음
// - 이유:
//   * static 함수 → this 포인터 없음 → 객체 멤버 변수 접근 불가
//   * const 함수 → 객체 멤버 변수 읽을 수 있음, 수정 불가
//   * 따라서 의미 충돌 → 컴파일 불가
// static 멤버 변수만 사용하는 static 함수라 해도 const는 붙일 수 없음
// 예:
// static double getArea() const; // ❌ 오류

// 6️⃣ 핵심 요약
// | 구분 | this 포인터 | 객체 멤버 접근 | static 멤버 접근 | const 사용 가능 |
// |------|------------|----------------|----------------|----------------|
// | 일반 멤버 함수 | ✅ 있음 | ✅ 가능 | ✅ 가능 | ✅ 가능 |
// | static 멤버 함수 | ❌ 없음 | ❌ 불가 | ✅ 가능 | ❌ 불가 |

// 💡 한 줄 요약
// static 함수는 객체 없이 클래스에 속하며, 객체 멤버 변수는 접근할 수 없고,
// static 멤버 변수만 사용할 수 있으며, const는 static 함수와 함께 사용할 수 없다.





3. 싱글톤
    클래스의 private: 접근지정자에 기본 생성자를 선언해두면 앞으로 내가 직접 객체 생성은 불가능하다.
    ex)class Member가 있는데, main문에서 Member a; 이렇게 생성할 수가 없다.
    그리고, 복사생성자와 복사 대입 연산자등을 delete한 상태로 static 클래스 객체를 생성하고 그 객체의 주소를 넘기는 static 멤버 함수를 선언하면 된다.
    예를들어 static멤버함수의 이름이 Instance()라고 하자. 이러면 main문에서 Member& a = Member::Instance(); 를 하면 a에는 새로 생성된 Member의 static 인스턴스가 담긴다.
    그 뒤로 내가 다른 Member b = Member::Instance();를 실행을 해도 앞서 생성된 객체를 b에다가 담게된다. 즉, 객체가 아무리 생성해도 전체적으로 하나밖에 없게되고 모두가 그것을
    공유하는 셈이다. 

    여기서 복사생성자는 말 그대로 생성할때 인자값으로 객체를 넣어주는 것이고, 복사 대입 연산자는 
    Member b; 를 생성한 상태에서
    b = a; 를 함으로서 a를 복사해서 대입하는 것이다.
    따라서 이걸ㄴ 함수이다.




### ----- 260310 -----
1. 클래스 상속
    상속 받으면 부모클래스의 속성과 기능을 자식클래스에서 그대로 사용할 수 있다.
    그리고 부모클래스에서 매개변수를 받는 생성자를 만들어놨다면 자식클래스에서는 단순한 기본 생성자는 사용할 수없다.
    왜냐하면 생성자는 상속이 되는 것이 아니기 때문이다.
    따라서 반드시 자식클래스의 생성자에서 부모클래스의 생성자를 생성자 초기화 선언해줘야 한다. 그러면 매개변수를 안받는 생성자, 매개변수를 부모클래스의 생성자와 동일한 갯수를 받는
    생성자, 매개변수의 갯수가 다른 생성자등등, 여러가지 생성자를 생성 가능하다.

2. override
부모클래스에 자식클래스를 담는 것은 가능하다. 왜냐하면 자식 클래스는 부모클래스의 모든 것을 가지고 있으니까.
그래서 부모클래스에서 virtual로 설정한 함수를 자식에서 override(재정의) 한다면 부모클래스 변수에서 자식클래스의 객체를 담고 있고 그 변수를 이용해서 부모클래스의
함수를 호출한다면 자식클래스에서 구현해두었던 virtual함수를 실행하게 된다. 
만약 virtual로 설정안했는데, 자식클래스에서 똑같은 이름의 함수가 있다면, 그건 부모클래스와는 전혀 다른 자식 클래스의 함수이다.
만약 자식클래스에서 부모 클래스의 함수를 사용하고 싶다면 부모클래스::함수이름() 이런식으로 사용하면 된다. 

3. 헤더파일과 cpp파일을 분할하여 클래스 컴파일 해보기
    - assert : <cassert> 라는 라이브러리를 통해서 사용가능
        - 실행중 특정 조건이 반드시 참이어야 한다는 것을 검사하는 디버깅용 매크로 함수. 거짓이면 프로그램을 중단(abort)한다.


4. 상속이 필요한이유
    - 상속이 없으면 각 클래스마다 공통적인 부분이 있어도 그것을 활용할수가 없다. 예를들어 고객이 물건을 사면 그 물건의 가격만큼의 돈을 감소시킨다 하자.
    - 이때, TV클래스도 Price라는게 있고, Computer도 Price라는게 있다. 그러면 고객이 물건을 살때 실행할 함수의 매개변수에
    - TV와 Computer 객체를 각각 받는 함수를 따로 만들어야한다.
    - 따라서 공통 부모클래스를 만들어 상속을 받아 매개변수로는 그 부모클래스 객체만 받게 하면 단 하나의 함수만으로 물건의 종류와 상관없이 함수를
    - 실행시킬수 있다.


5. Final
    - class 선언부의 끝에 Final을 선언하면, 상속이 안되게끔 막는다.

6. class 연관관계
    1. 소유(has - a)
    - class의 멤버 변수로서 다른 클래스의 객체를 가지고 있는것

7. 의존 관계
    1. 사용(use - a)
    - 상속과 연관보다는 약한 관계


### ----- 260311 -----

1. 추상클래스
    멤버함수를 자신이 전혀 구현하지 않고 자식클래스에서 구현시키는 클래스를 말한다.
    이 클래스는 객체화될수없다는 특징이 있다.
    ex) virtual void func() = 0;    이런식으로 하면 순수가상함수로서 작동한다. 자식클래스에서 반드시 구현해야한다.

2. 생성자는 상속이 되지 않기 때문에 virtual로 선언이 불가능하지만, 소멸자는 virtual로 설정 가능하다.
    - 이때 소멸자는 상속이 된다는 개념보다는 부모클래스의 포인터에 자식클래스를 담을때 그 포인터에 담긴 객체가 해제되면 자식클래스의 소멸자가 먼저 실행되고,
    - 부모클래스의 소멸자가 실행된다. 이때 소멸자를 virtual로 해두지 않으면 자식클래스의 소멸자가 실행되지 않고 부모의 소멸자만 실행된다.
    - 그래서 보통 자식에서 소멸자를 실행할게 있다고 한다면 부모의 소멸자를 virtual를 하는게 좋다.

3. 인터페이스 
    - 추상클래스와 동일하지만 변수없이 오로지 순수가상함수로만 이루어진 추상클래스를 인터페이스라고한다.
    - c#이나 다른언어와는 달리 인터페이스를 구현하는 문법은 따로 없고, 그저 사용자들 사이에서의 약속된 개념으로 작동한다.

4. 다중상속
    - 상속을 한개만 받는게 아니라 여러개를 받는 것을 말한다.
    - 문제는 상속을 받는것들이 동일한 클래스로부터 상속받아서 나온 클래스들이라면 
    - 변수들이 겹치게 된다. 따라서 나중에 사용할때 어느 클래스의 변수를 사용하는것인지 알 수가 없다.
    - 따라서 상속 받을때 단순히 public 클래스명 을 하는게 아니라 virtual public 클래스명을 선언해야한다.

5. 믹스인클래스

6. 예외처리


7. 배열을 또?
    
8. 템플릿
    - 헤더파일에서 템플릿을 이용한 함수를 선언한다고 하면 이건 cpp파일에서 구현할수없다.
    - 템플릿을 이용한 함수를 헤더파일에 선언하면 무조건 정의도 헤더파일에서 바로 해줘야한다.


### ----- 260312 -----
1. stl
   템플릿을 이용하여 구현하는 특별한 자료구조이다. 벡터, 스택, 큐, 리스트 ,셋, 맵 등등....

   - set : 중복된 원소를 허용하지 않고, 자동으로 정렬되는 자료구조 입니다.
   - map : map은 키-값 쌍으로 데이터를 저장하는 연관 컨테이너 입니다. 내부적으로 정렬된 상태로 저장되며, 키는 고유해야 합니다.

2. 


456 김예성 010-1234-5678 apple@naver.com
952 김세진 010-9876-5432 mango@gmail.com
746 황기태 010-6543-2110 strawberry@daum.com